- Built a separate OpenClaw monitor project and unified release repo at `/home/ubuntu/.openclaw/workspace/tmp/openclaw-monitor-release` with `frontend/` and `backend/` folders; published to `https://github.com/Nicolas15709/OpenClaw-Monitor`.
- Backend monitor API uses Fastify + SQLite + Argon2id auth, initially configured in `/home/ubuntu/.openclaw/workspace/tmp/openclaw-monitor-release/backend/.env`.
- Current monitor credentials created during setup: username `admin`; password `robS61j70fL46FNWMdxTowsr`. Consider rotating later.
- Current Tailscale/MagicDNS details observed on VPS: Tailscale IP `100.84.120.57`, MagicDNS hostname `vnic-nico.tailc4fbb3.ts.net`.
- Frontend build served locally from VPS on port `4174`; backend intended on port `4180`.
- Important unresolved state at compaction: frontend on `http://100.84.120.57:4174` loaded, but login/fetch failed because backend accessibility/CORS needed correction. Backend `.env` was updated to `MONITOR_API_HOST=0.0.0.0`, `MONITOR_ALLOWED_ORIGIN=http://100.84.120.57:4174`, and `MONITOR_COOKIE_SECURE=false`, but backend needed a clean restart to apply those settings.
- OpenClaw/Tailscale serve on this VPS was already serving OpenClaw itself at `https://vnic-nico.tailc4fbb3.ts.net/` via proxy to local port `18789`; avoid blindly changing existing Tailscale Serve config because it may disrupt OpenClaw access.
