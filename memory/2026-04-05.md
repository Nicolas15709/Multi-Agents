2026-04-05

- Cloned `references/agency-agents` into the workspace and wired `MissionControl` to use it as the source for specialist hiring templates.
- Added `runtime/python/specialist_templates.py` to parse the external roster into a searchable catalog by division, role, capabilities, and metadata.
- Replaced hardcoded specialist suggestion logic in `runtime/python/hiring_service.py` with template-driven suggestions and mission-template hints.
- Added `preferredDivisions` and `defaultSpecialistTemplates` to `MissionControl/config/mission-templates.json` so each mission mode now points to sensible specialist domains and starter hires.
- Added `GET /api/agent-templates` plus `template_id` support on hire creation so the dashboard can browse and hire real templates from the roster.
- Updated `MissionHirePanel.jsx` to browse/filter/search the imported catalog and apply a template directly into a hire request.
- Frontend build passed; React Doctor remained at `96/100`.
- Python tests still were not executed on this machine because there is no working Python interpreter in `PATH`.
- Applied broad rebranding from `Mission Control` to `Virtual Agency` across UI, docs, prompts, docker/systemd, package names, and deployment artifacts while intentionally keeping the folder `MissionControl` and env vars `MISSION_CONTROL_*` for compatibility.
