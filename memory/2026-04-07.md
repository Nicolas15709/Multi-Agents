## 2026-04-07

- Confirmed and fixed root `package.json` legacy script paths to `src/legacy/main_orchestrator.js`.
- Found that the dashboard could show stale local state because `useSnapshot` silently fell back to `MissionControl/apps/dashboard/public/snapshot.json` when the runtime API failed.
- Changed local snapshot behavior so the static fallback is now opt-in only via `VITE_ENABLE_STATIC_SNAPSHOT_FALLBACK=true`.
- Confirmed runtime auth env is loaded from `MissionControl/.env`, not the workspace root `.env`.
- Fixed runtime agent-state idempotency bug (`planning -> planning`) and re-ran Python tests successfully.
