# 2026-04-06

- Llevamos `MissionControl`/`Virtual Agency` bastante mas cerca de `paperclip`: planner abierto por capacidades, tracks dinamicos por dominio, capability router, action approvals, action budgets, shared mission memory y agent handoffs persistentes.
- Nuevos modulos clave del runtime: `mission_blueprints.py`, `capability_router.py`, `coordination_service.py`, `approval_service.py`.
- `task_runner.py` ahora bloquea acciones externas por presupuesto/aprobacion y adjunta contexto de coordinacion antes de iniciar tareas.
- `hiring_service.py` ya sugiere especialistas por gaps reales de capacidades del plan, no solo por templates preferidos.
- Snapshot/API ahora exponen `sharedMemory`, `agentMessages`, `actionApprovals` y `action_budgets`.
- Frontend validado despues de estos cambios: `npm run build` OK, React Doctor `96/100`.
- Se instalo Python 3.11 localmente, se creo `.venv`, se instalaron dependencias del runtime y la suite Python completa quedo verde: `114 passed`.
- Tambien se corrigieron imports package/script-mode del runtime para que `pytest` funcione desde `MissionControl` sin shadowing con otro `runtime/` del workspace.
